(() => {
    'use strict';
    /**
     * @ngdoc module
     * @name module
     * @requires dependencies
     * @description
     *
     * The `module` description.
     *
     */
    angular
        .module('module', [
            'dependencies'
        ]);
})();
